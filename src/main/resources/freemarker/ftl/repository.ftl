package ${repositoryUrl};

/**
 * @Description:TODO(${entityComment}Dao层)
 *
 * @version: ${version}
 * @author: ${author}
 * @date: ${createTime}
 */
@Repository
public class ${entityName}Repository {
    @Autowired
    private ${entityName}Mapper ${'${entityName}'?uncap_first}Mapper;

}
